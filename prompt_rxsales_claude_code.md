# مهمة: دمج «الكتابة مقابل الصرف» في PharmaDash

أنت تعمل على ملفين موجودين في مجلد المشروع:

1. `PharmaDash_Pro_offline_3.html` — الداشبورد الرئيسية (ملف bundle واحد، offline بالكامل). **هذا الملف هو الذي ستعدّله.**
2. `sales-forecast-dashboard.html` — داشبورد مبيعات منفصلة، **للقراءة فقط** (مرجع لفهم صيغة بيانات المبيعات). لا تعدّلها.

الهدف: إضافة شاشة جديدة في PharmaDash اسمها **«الكتابة مقابل الصرف»** تقارن كتابات الأطباء (من ملفات أوراكل المحمّلة أصلًا في PharmaDash) مع المبيعات الفعلية (تُستورد كملف JSON من داشبورد المبيعات)، وتُظهر التسرب بالوحدات وبالقيمة المالية.

---

## 0) بنية الـ bundle — اقرأ هذا قبل أي تعديل

`PharmaDash_Pro_offline_3.html` ليس HTML مباشرًا. بنيته:

- سكربت loader + `<script type="__bundler/manifest">` (أصول base64+gzip: Chart.js, xlsx, pdf.js ×2, خطوط) — **ممنوع لمسها نهائيًا**.
- `<script type="__bundler/template">` — التطبيق الفعلي مخزَّن **كسلسلة JSON واحدة** (اللودر يعمل `JSON.parse(templateEl.textContent)`).

**خط أنابيب التعديل الإلزامي (Python):**

```python
import re, json
bundle = open('PharmaDash_Pro_offline_3.html', encoding='utf-8').read()
m = re.search(r'(<script type="__bundler/template">)(.*?)(</script>)', bundle, re.S)
app = json.loads(m.group(2).strip())          # ← HTML التطبيق الحقيقي

# ... طبّق التعديلات على app بسلسلة replace مع تأكيد أن كل anchor يظهر مرة واحدة:
def rep(old, new, label):
    global app
    assert app.count(old) == 1, f'ANCHOR FAIL [{label}]: {app.count(old)}'
    app = app.replace(old, new)

# إعادة التغليف:
tpl = json.dumps(app, ensure_ascii=False).replace('</', '<\\/')   # حرج: يمنع كسر وسم السكربت الخارجي
assert json.loads(tpl) == app
out = bundle[:m.start(2)] + '\n' + tpl + '\n' + bundle[m.end(2):]
assert json.loads(re.search(r'<script type="__bundler/template">(.*?)</script>', out, re.S).group(1).strip()) == app
open('PharmaDash_Pro_offline_3.html', 'w', encoding='utf-8').write(out)
```

داخل `app` توجد 7 كتل `<script>` تنتهي بـ `<\u002Fscript>` (هذا طبيعي — لا تغيّره). الكتلة الرئيسية للتطبيق هي الخامسة (index 4) وتحتوي IIFE فيه STATE وكل الشاشات. كتلة blk1 تحتوي PharmaCore (parsers + entityKey + sameEntity) كدوال top-level عامة.

**بعد كل تعديل:** استخرج الكتل السبع (بعد استبدال `<\u002Fscript>` بـ `</script>` في نسخة عمل) ومرّر كل واحدة على `node --check`، وتأكد من عدم وجود دوال مكررة الاسم.

---

## 1) صيغة بيانات المبيعات (المصدر)

اقرأ في `sales-forecast-dashboard.html` الدوال `loadEntries` و`backupData` و`const PRODUCTS` لتتأكد بنفسك من الصيغة الفعلية. ملخصها:

- التخزين: `localStorage['private_label_sales_dashboard_v1']` = مصفوفة إدخالات.
- كل إدخال: `{ id, date:'YYYY-MM-DD', pharmacy:'صيدلية 1|صيدلية 7|صيدلية 18', product:<key>, units:Number, price:Number, cost:Number, prescriptions:Number }` — تحقّق من الأسماء الفعلية للحقول من الكود، وإن وُجد اختلاف اتبع الكود لا هذا الملخص.
- زر «نسخة احتياطية» في تلك الداشبورد يصدّر JSON بهذه المصفوفة — **هذا هو الملف الذي سيستورده PharmaDash**. يجب أن يقبل PharmaDash الصيغتين: مصفوفة مباشرة، أو كائن يحتوي المصفوفة داخل حقل (افحص backupData لتعرف الغلاف الفعلي).

### مفاتيح الأصناف (13) — موحّدة بين النظامين
`reflex, rizer, oracure, intimo, nostriderm, nostricure` (Private Label)
`pure_omega, purefeel, centrozon, centrozon_women, zincolive, bonefriend, argigrow` (PureHerb — تحقّق من المفاتيح الحرفية من const PRODUCTS).

### ربط الفروع (مُثبَّت — لا تخمّن)
```js
const SALES_BRANCH_MAP = { 'صيدلية 1':'T1', 'صيدلية 18':'T2', 'صيدلية 7':'T3' };
```
(ص1 = التعاون الأول T1، ص18 = التعاون الثاني T2، ص7 = التعاون الثالث T3.)

---

## 2) المرحلة 1 — استيراد ملف المبيعات

- وسّع مُدخل الملفات الحالي (`fileInput` + منطق onFile) ليقبل `.json`. اكتشاف النوع: إن كان JSON يحتوي مصفوفة إدخالات فيها `date` بصيغة `YYYY-MM-DD` و`pharmacy` و`product` → ملف مبيعات.
- خزّنه في `STATE.sales = { entries:[...], loadedAt, fileName }` **وليس** داخل فرع معيّن (كل إدخال يحمل فرعه)، مع حفظ في `localStorage['pharmaDash.sales.v1']` واستعادته عند الإقلاع (داخل try/catch — كما تفعل بقية المفاتيح).
- تحقّق (validation): تجاهل الإدخالات غير الصالحة بصمت مع عدّها، واعرض toast/بطاقة ملخص: عدد الإدخالات الصالحة، نطاق التواريخ، الفروع، عدد الأصناف. زر «إزالة ملف المبيعات».
- رفع ملف جديد يستبدل القديم بالكامل (لا دمج في v1).

## 3) المرحلة 2 — شاشة «الكتابة مقابل الصرف»

أضف عنصر تنقّل جديدًا (نفس نمط nav-item الحالي) بمفتاح `rxsales` وأيقونة SVG بنفس أسلوب ICO الحالي.

### نافذة المطابقة الزمنية
- خذ أحدث فترة للفرع النشط عبر `periodInfo(p.name)` — تعيد `{score, sure, label}` وتفهم أسماء مثل `01-07_to_20-07.pdf` (نطاق يوم-شهر بلا سنة). استخرج من الاسم نطاق الأيام والشهر، وكوّن نافذة `[بداية, نهاية]` بصيغة YYYY-MM-DD (السنة: من إدخالات المبيعات نفسها — اختر سنة الإدخالات التي شهرها يطابق شهر الفترة؛ وإن تعذّر فالسنة الحالية).
- فلتر إدخالات المبيعات: `entry.date` داخل النافذة **و** `SALES_BRANCH_MAP[entry.pharmacy] === الفرع النشط`. لو الفرع النشط ALL → كل الفروع.
- اعرض النافذة المستخدمة بوضوح أعلى الشاشة: «نافذة المطابقة: 1–20 يوليو · التعاون الأول».

### الكتابات لكل صنف (من أوراكل)
- من `activeData().rows` (أحدث فترة): صنّف كل صف على مفتاح صنف عبر مطابقة `r.service` — أعد استخدام المنطق الموجود (`findPrivateLabelProduct` ومطابِق PureHerb) وابنِ فوقه `productKeyOf(service) → key|null` يغطي الـ 13 مفتاحًا. لا تعِد كتابة قواعد المطابقة من الصفر.

### KPIs (بطاقات بنفس نمط kpi الحالي، مع data-to للعدّاد)
1. كتابات الفترة (مجموع الأصناف الـ13)
2. وحدات مصروفة (مجموع units في النافذة)
3. معدل التحويل % = مصروف ÷ مكتوب (نقطة عشرية واحدة)
4. فجوة التسرب بالوحدات = مكتوب − مصروف (لا تقصّها عند الصفر؛ لو سالبة اعرض «صرف أعلى من الكتابة» بلون مختلف)
5. إيراد مُهدَر = Σ لكل صنف: max(0, مكتوب−مصروف) × متوسط سعر البيع المرجّح بالوحدات لذلك الصنف من إدخالات النافذة (fallback: متوسط بسيط، ثم 0 مع شارة «لا سعر»)
6. هامش مُهدَر = نفس الحساب بـ (سعر − تكلفة)

### الرسم الرئيسي (Chart.js — موجود في الـ bundle)
- **منحنى الصرف التراكمي اليومي** داخل النافذة (line, fill خفيف) ضد **خط أفقي متقطع = إجمالي كتابات الفترة**. المسافة بينهما = التسرب.
- استخدم توكنز الثيم الحالية (tickColor/gridColor حسب data-theme) + `destroy` قبل إعادة الإنشاء بنفس النمط المتبع + زر تصدير PNG عبر `wireBoard`/`exportChartPNG` الموجودتين.

### أعمدة لكل صنف
- Bar مجمّع: «كتب» و«صُرف» لكل صنف (الـ13 أو الموجود منها فقط)، مرتّبة تنازليًا بفجوة التسرب، مع tooltip يظهر التحويل%.

### جدول التسرب (فرع × صنف)
أعمدة: الصنف · الفرع · كتب · صُرف · تحويل% · فجوة · إيراد مُهدَر · هامش مُهدَر. Severity بنفس عتبات داشبورد المبيعات: تحويل <40% أحمر، <80% كهرماني، وإلا أخضر. فرز افتراضي بالفجوة.

### ربط الأطباء (القيمة الذكية)
- لأعلى 3 أصناف تسربًا: شرائح (chips) بأعلى 5 أطباء كتابةً لهذا الصنف في الفترة (من rows)، بالضغط تفتح ملف الطبيب عبر `openDoctorFrom(name, prevData, prevLabel)` الموجودة. العنوان: «الصنف يُكتب ولا يُصرف — راجع التوافر أو تسرب المرضى للخارج».

### حالات فارغة
- لا ملف مبيعات → بطاقة إرشادية: «صدّر نسخة احتياطية من داشبورد المبيعات وارفع ملف JSON هنا».
- لا فترة أوراكل → نفس نمط الرسائل الحالي.
- مبيعات موجودة لكن خارج نافذة الفترة → وضّح ذلك بدل عرض أصفار صامتة.

---

## 4) قواعد هندسية غير قابلة للتفاوض (نمط المشروع)

1. **ممنوع** template literals متداخلة. بناء HTML بسلاسل `'` + `+` فقط.
2. كل نص ديناميكي في HTML عبر `esc()` وكل attribute عبر `escAttr()` (موجودتان).
3. الألوان/الأبعاد عبر CSS variables الموجودة فقط — الدارك مود يعمل تلقائيًا. أي كلاس جديد أضف تعريفه في كتلة `<style>` بنفس التوكنز (راجع `.alert-card` كنموذج).
4. RTL: أي نطاق أرقام بسهم (مثل `12 → 5`) لُفّه في `dir="ltr"`.
5. لا مكتبات جديدة ولا CDN — الملف offline. Chart.js وxlsx وpdf.js موجودون في الـ manifest.
6. لا أسماء دوال مكررة عبر الكتل. الدوال الجديدة داخل IIFE الكتلة الرئيسية (blk index 4).
7. `prefers-reduced-motion` محترم في أي حركة جديدة (استخدم `noMotion()` الموجودة).
8. loader + manifest + ذيل الملف تبقى **مطابقة بالبايت** — عدّل الـ template فقط.

---

## 5) الاختبار الإلزامي قبل التسليم (jsdom)

ثبّت jsdom وشغّل الملف **النهائي المسلَّم نفسه** (بعد إعادة التغليف). وصفة الحاضنة — كل بند منها درس مدفوع الثمن، لا تتخطَّ أيًا منها:

```js
const {JSDOM}=require('jsdom');
// 1) فك الـ template بـ JSON.parse من الملف النهائي
// 2) بدّل <script src="UUID"> بالأصل المفكوك inline — بدالة replacer: html.replace(re, ()=>'<script>'+code+'</script>')
//    (بدونها رموز $ داخل xlsx تفسد الاستبدال). احذف Chart.js إن أردت اختبارًا بلا canvas — الرسوم تنسحب بأمان.
// 3) خيارات JSDOM إلزامية: { runScripts:'dangerously', pretendToBeVisual:true, url:'https://x.local/' }
//    بدون url حقيقي: localStorage يرمي SecurityError (opaque origin).
// 4) beforeParse polyfills: matchMedia stub، structuredClone، ReadableStream/WritableStream/TransformStream،
//    DOMMatrix وPath2D فارغتان — مطلوبة لتشغيل pdf.js (fake worker؛ تحذير "Setting up fake worker" طبيعي).
// 5) رفع ملف: new File([...], name) ثم Object.defineProperty(input,'files',...) ثم dispatch change.
//    مع PDF: انتظر حتى يظهر اسم الملف في branchBody **ويختفي** نص «جارٍ تحليل» (وإلا سترفع الثاني أثناء parsing الأول).
// 6) عدّادات count-up تجعل قراءة الأرقام أثناء الحركة خاطئة — انتظر >1s بعد الرندر قبل التحقق من قيم نهائية.
```

### fixture حتمي — تحقّق من هذه الأرقام بالضبط
ارفع CSV أوراكل صناعيًا باسم `01-07_to_20-07.csv` بالأعمدة `Patient No,Patient Name,Service Name,Order No.,Status,Doctor Name,Section Name`:
- د. AHMED ALI يكتب `REFLEX CREAM` ×10
- د. SARA HASSAN تكتب `ORACURE GEL` ×5
- د. OMAR SAID يكتب `PANADOL` ×5 (خارج الأصناف — يجب ألا يدخل الحساب)

وملف مبيعات JSON بإدخالات داخل 2026-07-01..20 على «صيدلية 1»:
- reflex: units=6, price=40, cost=22
- oracure: units=5, price=30, cost=18

**النتائج المتوقعة في شاشة «الكتابة مقابل الصرف» (فرع T1):**
- كتابات = 15، مصروف = 11، تحويل = 73.3%
- فجوة = 4 (كلها reflex)
- إيراد مُهدَر = 4×40 = **160**، هامش مُهدَر = 4×18 = **72**
- صف reflex في الجدول: تحويل 60.0% (كهرماني)، صف oracure: 100% (أخضر)
- شرائح الأطباء لأعلى صنف تسربًا (reflex) تعرض AHMED ALI وتفتح ملفه عند الضغط
- إدخال مبيعات بتاريخ 2026-06-25 (خارج النافذة) **لا** يُحتسب
- إدخال بفرع «صيدلية 7» لا يُحتسب مع فرع T1 النشط، ويُحتسب مع ALL

### اختبارات سلامة عامة بعد التعديل
- الكتل السبع تعدّي `node --check`، لا دوال مكررة.
- جولة على كل عناصر التنقل (14+1) + تبديل الثيم = صفر أخطاء console.
- الشاشات القديمة (المقارنة الزمنية، الأطباء، ملف الطبيب) لم تتأثر — أعد فحصها سريعًا.
- localStorage للمبيعات يعيش بعد إعادة تحميل (أنشئ JSDOM ثانية بنفس الـ url وتأكد أن STATE.sales استُعيد).

---

## 6) التسليم

- `PharmaDash_Pro_offline_3.html` محدّثًا (نفس الاسم).
- سجل اختبار مختصر: مخرجات الـ fixture أعلاه كما ظهرت فعليًا.
- قائمة الـ anchors التي عُدّلت (من مخرجات rep) لأراجعها.

اعمل على مراحل: (أ) الاستيراد والتخزين، اختبر؛ (ب) الشاشة والحسابات، اختبر بالـ fixture؛ (ج) الرسوم والجدول والربط بالأطباء، اختبر الجولة الكاملة. لا تنتقل لمرحلة قبل نجاح اختبار سابقتها.
